//switch option 
const option = 2;

switch (option) {
    case 1: console.log("english");
    break; // break is used to exit the switch statement they stop the moving forward 
    case 2: console.log("spanish");
    break;// if we dont use
    case 3: console.log("french");
}